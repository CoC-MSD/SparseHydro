"""Interactive Streamlit viewer for :mod:`sparsehydro.pipeline` results.

Requires the ``viewer`` extra (``pip install sparsehydro[viewer]``). Launch
with the ``sparsehydro-viewer`` console script; see :mod:`sparsehydro.viewer.cli`.

The Streamlit-free helpers in :mod:`sparsehydro.viewer.data` (global-event
table, filtering, plot windows) can be imported without Streamlit installed.
"""
